 <!-- 1、展示轮播图 -->
 <w-swiper list="{{banners}}"  />
 <!-- 2、推荐数据展示 -->
 <view class="recommend">
  <block wx:for="{{recommends}}" wx:key="{{indx}}">
    <view class="recommend-item">
    <image src="{{item.image}}"></image>
    <view>{{item.title}}</view>
    </view>
  </block>
 </view>